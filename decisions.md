# Eletron — Decision Log

Traceability record of architectural and product decisions made during the build.
Each entry records what was decided, why, and when.
Full design spec: `docs/superpowers/specs/2026-07-07-eletron-design.md` (+ appendix with the complete data/UX/security designs).

---

## Architecture Decisions

| # | Decision | Chosen | Alternatives Considered | Rationale |
|---|---|---|---|---|
| 1 | Platform (2026-07-07) | Next.js 15 (App Router) on Vercel GRU | Electron desktop app | Workspace convention (VammoGrid/goBuy/VammoTestFleet); phone access needed for the meter-photo flow; OAuth gating is a workspace hard rule for Vercel apps; "eletron" is the project name (energy theme), not the framework |
| 2 | Authentication | next-auth v5 beta + Google OAuth restricted to @vammo.com, from day one | Public unlisted URL (VammoGrid V1 style) | Workspace hard rule for anything on Vercel; goBuy's `auth.ts`/`middleware.ts` is the proven template (hd hint + server-side endsWith check + email_verified) |
| 3 | v1 data backbone | UI-first: live Google Sheets read (cached ~15 min) + xlsx fixtures; Supabase arrives in Phase 2 | Supabase from day one; replace scraper outputs entirely | Gabriel's call: see the app take shape first ("for now, only worry about data flow and app format/function"). A repository interface + a single `normalize.ts` mean the data layer is reused when Supabase lands, not rebuilt. Replacing scraper outputs violates "scraper stays as-is" |
| 4 | Domain model | station 1—N `billing_accounts` (rent / energy_enel / energy_edp / third_party) 1—N `charges` (+ `charge_lines`, `payments`, `receipts`, `meter_readings`) | Flat per-station bill columns (sheet mirror) | Evidence from the sheets: 27 stations have 2–3 utility installations (station 553 has 3); Hubees bills ~16 stations in one nota de débito; condo/Kitchen Central documents mix rent+energia and need per-line splits; UNIDENTIFIED rows need a first-class unmatched state |
| 5 | External keys | `swap_station_id` (int) universal; `enel_id` / `edp_uc` / `cadastro_id` / counterparty CNPJ per account type; CNPJ digits-only is the issuer key, razão social never is | Name-based matching | Rede Automan 1 vs 2 have near-identical names and different CNPJs; Kitchen Central SPEs have standardized names; matching by name already produced the UNIDENTIFIED debris in the sheets |
| 6 | Dedupe/idempotency | `content_hash` (sha256) unique on documents; `linha_digitavel` for boletos; `(enel_id\|uc, due_date)` for scraper invoices; per-source `dedupe_key` unique on charges; filenames are never identity | Filename-based tracking (status quo) | Byte-identical bill PDFs exist under different filenames in the current inbox; the scraper sheet's own dedupe invariant is `(id, due_date)` |
| 7 | Writes (Phase 2+) | SECURITY DEFINER RPCs only + `audit_events` in the same transaction; zero client INSERT/UPDATE/DELETE; two roles (`admin`, `operator`) checked inside Postgres | Direct table grants; app-layer-only checks | goBuy `finance.request_events` pattern is the workspace standard; Postgres as final authority; "mark paid" and "remap account" have different blast radii, hence two roles |
| 8 | AI trust boundary (Phase 2+) | `record_payment` structurally refuses charges not human-confirmed; AI-extracted data always lands in review queues first | Auto-apply AI extractions | Nothing AI-written may reach "pago" without a named human in the audit trail; the n8n flow's silent UNIDENTIFIED appends are the failure mode being fixed |
| 9 | File storage (Phase 2+) | ONE private Supabase Storage bucket `eletron-documents`, path `{kind}/{yyyy}/{mm}/{sha256}`, served only via session-checked signed URLs (5 min TTL) | Four per-kind buckets; keep Drive anyone-with-link | No client storage policies at all, so bucket-level separation adds nothing; fixes the anyone-with-link exposure for app-served files. Later task: flip the Drive folder itself off anyone-with-link |
| 10 | Scraper relationship | Vammo-Enel stays untouched; its sheet tabs are pipeline-owned read-only inputs; columns resolved by header name only; all pt-BR parsing quarantined in `normalize.ts` | Modify scraper to write Postgres | Scraper has its own read-back logic (sheet dedupe, Drive-PDF idempotency) and a human Slack matching loop; breaking it risks the nightly run. Revisit post-v1 as a scraper-side change |
| 11 | Component library | `@leopardaelectric/vammo-ui` first (Sidebar, PageHeader, TableControls, StatCard, badges); bare shadcn/ui only for gaps; Vammo DS Product track (Inter, 8/12 px radius, alert palette) | Bare shadcn/ui like VammoGrid/goBuy | Workspace gotcha says check vammo-ui before adding UI components; eletron becomes the first Desktop/Vammo project to actually consume it |
| 12 | Rollout | 3 phases: (1) read-only visibility + meter-reading UI; (2) Supabase + sync + first writes (meter readings, comprovantes, alert lifecycle); (3) AI ingestion + contract onboarding | Everything in v1 | Each phase ships something usable; risky writes come after the foundation hardens; approved by Gabriel 2026-07-07 |
| 13 | n8n migration | Design-for-replacement now, migrate later; every workflow has a designated home (see spec §n8n); the two read-only alert workflows are cut over first after parity; Arqia SIM stays in n8n (out of scope — IoT telemetry, not station finance) | Replace n8n in v1; coexist indefinitely | Gabriel's call 2026-07-07; lowest-risk order is read-only alerts → comprovantes → email ingestion → cadastro form |
| 14 | Vercel deployment (2026-07-07) | GitHub repo `GMBeltrami-Vammo/eletron` connected to Vercel project `eletron` (auto-deploy on push); local working copy linked via `vercel link` for CLI access (`vercel env pull`, `vercel dev`, preview deploys) | Deploy via CLI only, no GitHub integration | Matches decision #1 (Next.js on Vercel); GitHub integration gives PR preview deploys, CLI link enables pulling env vars locally once Supabase/next-auth secrets exist in Phase 2 |
| 15 | Dev-only login bypass (2026-07-07) | Credentials provider `dev-login` in auth.ts, active only when `NODE_ENV=development` AND `AUTH_DEV_LOGIN=1` (.env.local only, gitignored) | Test against real Google OAuth locally; no bypass | Local UI verification needs a session without a Google round-trip; the double guard makes production activation structurally impossible; Vercel env never carries the flag |
| 16 | Canonical "vencida" (2026-07-07) | ONE overdue definition everywhere = the `overdue_bill` alert rule (status/history Vencida MINUS EDP bills whose Ultimo Comprovante is dated on/after the due date) | Raw `billStatus === 'vencida'` per surface | First live run showed the dashboard KPI (3) disagreeing with the quick-filter chip (0) for the same label; the raw status recounts already-receipted EDP bills that n8n VencidasEnelWarning deliberately skips. Workspace rule: one canonical definition per concept |
| 17 | Phase 2 file store (2026-07-08) | Google Drive, not Supabase Storage: meter photos → `1t7WoW…`, comprovantes → `13nbLPM…` (n8n watch folder), manual-bill PDFs → scraper bills folder `1AB8ok…` with scraper naming; Supabase keeps metadata + `drive_file_id`; photos/comprovantes served via session-checked proxy `/api/files/[documentId]`; anyone-with-link continues ONLY for manual-bill PDFs (scraper parity) | Private Supabase Storage bucket (decision #9) | Gabriel's call: files live where the team already works (Drive) and where n8n/scraper ecosystems expect them. SUPERSEDES the storage half of decision #9; the signed-URL half survives as the Drive proxy |
| 18 | Supabase location (2026-07-08) | New `charging` schema on the shared "Vammo Automations" project (`jfdqlnpidynxwqqiblcd`, PG15, also hosts goBuy `finance` + CX). Isolation: RLS everywhere, zero client write grants, `REVOKE EXECUTE` on functions from PUBLIC/anon, writes only via SECURITY DEFINER RPCs; minted JWTs carry `app:'eletron'` and charging policies require it (goBuy tokens can't read charging) | Dedicated Supabase project; `eletron` schema name | Gabriel's call (schema named for the team, charging); one project to operate; goBuy's proven auth bridge; the app claim closes the shared-JWT-secret leakage hole |
| 19 | Sheet write policy (2026-07-08) | AMENDS decision #10/CLAUDE.md: the app MAY write to the scraper spreadsheet, but ONLY append-style rows to `Faturas_ENEL`, `Faturas_EDP` and `2_Pagamentos`, only through `lib/sheets/faturas-writeback.ts` (tab allowlist asserted in code, outbox `sheet_writebacks` with retries; DB is source of truth, append failure never rolls back). Scraper-owned state tabs (enel_data/edp_data/Vammo_data) remain untouchable. `6_senhas` moved out of the sheet at setup (SA Editor is spreadsheet-wide) | Keep absolute read-only; Supabase-only writes | Parallel-transition philosophy: the sheet ecosystem (n8n comprovantes, Apps Script fiscal exports A1/A2/A6) must keep receiving manual bills and generated rent rows until cutover |
| 20 | Charge identity (2026-07-08) | ONE dedupe recipe per logical charge regardless of creator: manual bills reuse `enel:{id}:{due}`/`edp:{uc}:{due}` (`source='manual'`); gerar_mes reuses `pag:{cadastro_id}:{YYYY-MM}:aluguel`; `status_source ('sync'\|'rpc')` makes human writes sticky against re-sync | Per-creator keys (`manual:{uuid}`, `rent:{…}`) | Adversarial review C1: per-creator keys + sheet writeback = guaranteed duplicate charges on the next sheet-sync of the app's own written-back rows |
| 21 | Fiscal semantics (2026-07-08) | `Financeiro Check` = "exported to the FISCAL spreadsheet" (modeled `fiscal_exported`, labeled "Enviado ao fiscal"), NOT paid. `charge.status` re-derives from `utility_account_state.billStatus` + Comprovante cell + invoice history + decision #16's EDP rule; `pago` only via `confirm_charge`/`record_payment` RPCs or a genuine paid signal | Keep fin_check→pago derivation (Phase 1 behavior) | Gabriel's Apps Scripts (A1/A2) revealed the true meaning: the flag is set by the fiscal-export automation, not by payment. Fiscal export itself stays sheet-side until Phase 3 |
| 22 | Job scheduling (2026-07-08) | n8n is the scheduler (3 Schedule→HTTP workflows with `CRON_SECRET`: sheet-sync 3×/day, comprovantes-poll */15, alerts-eval chained); the single Vercel Hobby daily cron is a catch-up endpoint (sync+poll+alerts in one Fluid 300s run); `sheet_sync_stale` self-alert >26h. PRECONDITION: confirm n8n hosting is always-on | Vercel Cron only; Supabase pg_cron | Vercel Hobby = 2 crons, 1×/day each — the needed cadence is impossible without Pro; n8n is already in-stack |
| 23 | Supabase auth bridge (2026-07-08) | Mint the Supabase JWT per-call, server-side only (goBuy pattern), inside server actions/routes after `await auth()`; NO token in the NextAuth session/cookie | jwt/session callbacks storing the token in the session | Adversarial review H5: session-exposed 8h bearer usable against PostgREST from anywhere + edge cookie re-encoding complexity, for zero functional gain |
| 24 | Phase 2 UX write gates (2026-07-08) | Auto-matched comprovantes land as `conciliado` (never `pago`); only `confirm_charge` by a named operator flips to `pago`; offline queue for leituras explicitly deferred (authenticated multipart + session-expiry complexity); meter-reading responsible = logged-in user | Auto-apply matches; offline-first queue; selectable responsible | Decision #8's AI trust boundary applied to the matcher; simplicity for v2 of the phone flow |
| 25 | Phase 2.5: sheets severed (2026-07-08) | ONE final clone of the sheets into `charging` at the R2 cutover ritual, then the app never reads or writes any Google Sheet again; `REPOSITORY_BACKEND=supabase` becomes the permanent default; manual bills keep only the Drive PDF upload; scraper/portal data FREEZES in this test environment (accepted — a scraper→Supabase feed is a future scraper-side project). NB: re-run migration 8's `rent_manual` seed after the final clone (it ran against an empty contracts table) | Keep sheet-sync ingestion; keep Faturas_*/2_Pagamentos writebacks | Gabriel: "Clone the sheets to supabase and drop them entirely. This will be a test environment." SUPERSEDES #3 (sheets backbone) and the writeback half of #19 (`faturas-writeback.ts` + outbox dormant); #10's "scraper stays untouched" survives — it just no longer feeds this app; #22's sheet-sync schedules die (route deleted; sync core kept only for the final clone via `scripts/backfill.ts`) |
| 26 | Roles suspended (2026-07-08) | `is_operator()`/`is_admin()` redefined to `is_vammo_user()` (migration 8); app-side `isOperatorEmail`/`withAdmin`/`getViewer` pass any @vammo.com session; /admin Usuários card hidden; `user_roles` + `set_user_role` kept dormant as the restoration point | Keep the two-role model | Gabriel: "Remove any role for now, there is no need for them." AMENDS #7 — RPC guard structure and audit trail intact, only the role check collapses |
| 27 | Email-cobrança ingestion via app webhook (2026-07-08, ships at R2) | n8n keeps Gmail + LlamaParse + AI + Drive upload and POSTs its AI output to `POST /api/ingest/cobrancas` (Bearer `N8N_INGEST_SECRET`), replacing its `2_Pagamentos` append; MATCHED aluguel cobranças claim the `pag:{cadastro}:{YYYY-MM}:aluguel` dedupe key (converges with gerar_mes both ways); everything webhook-touched lands `needs_review` for human check/reclassify (`reclassify_charge` RPC) | App-side Gmail polling; keep the n8n→sheet append | Decision #8's trust boundary + #20's one-recipe-per-charge applied to the email flow; Gabriel edits the n8n node against our payload contract |
| 28 | Metabase direct sync (2026-07-08) | Daily `runMetabaseSync` pulls cards 28816 (stations) + 28556 (installed boxes) with `METABASE_API_KEY` and updates/inserts `stations` (status/name/address/active_boxes/boxes_synced_at); daily cron chain = metabase-sync → alerts-eval → comprovantes sweep; `scraper_stale`/`sheet_sync_stale` alert rules retired and bill-level alert rules gate on `scraped_at` ≤ 30 days (frozen data goes quiet) | Keep reading boxes via the sheet's 4_Metabase_Boxes tab | Keeps "matches our Metabase" (requests 1/3) alive with zero spreadsheet intermediary after the sever |
| 29 | Paid ⟺ comprovante bound (2026-07-09) | An entry is `pago` if and only if a comprovante is bound (deterministic matcher OR manual link). The auto-matcher now flips a linked charge straight to `pago` (was `conciliado`); `record_payment` only reaches `pago` when a payment has a bound `receipt_id` (migration 14) and `confirm_charge` likewise requires a bound receipt (migration 15) — a receiptless "registrar pagamento" records the payment but does NOT give baixa. `conciliado` becomes a legacy state. Worry rules: energy bills WITH DA worry only after due date, WITHOUT DA 7 days before, and both are suppressed once the account has a settled energy charge for the month; new `rent_payment_due` alert for pix/transferência rent generated-but-unpaid after the 5th (migration 13) | Keep the conciliado→confirm_charge human gate (decision #24) | Gabriel 2026-07-09: "if and only if a payment has a comprovante binded to it, it is marked as paid." AMENDS #8/#24 — the AI trust boundary relaxes because comprovante parsing is fully deterministic (no AI); a unique-key auto-match is trusted to mark paid, ambiguous still → needs_review |
| 30 | Contract onboarding intake (2026-07-09) | n8n (`Fill_Cadastro_Form`) keeps the OpenAI extraction + Metabase station lookup and POSTs the JSON to `POST /api/ingest/contratos` (Bearer `N8N_INGEST_SECRET`), replacing the Google-Form-prefill + Slack tail. The app downloads the PDF, dedupes a `documents` row, and STAGES the extraction in `charging.contract_intake` (`pending`); a human reviews at `/revisao/contratos` and confirms → `confirm_contract_intake` RPC creates the contract + `locador` counterparty + rent billing_account (or `reject_contract_intake`). Station: attaches to an existing `swap_station_id` only — the app NEVER creates stations (#28). CPF/CNPJ normalized (leading zeros). `status_da_locacao` prefill map (Ativa→ACTIVE, Em negociação→PRE_INSTALLATION, Suspensa→INACTIVE) is a human-overridable default (migration 16) | Create the contract directly in a draft status; app creates stations | Gabriel provided the n8n workflow + sample PDFs; staging table avoids polluting `contracts` (whose status enum has no "draft"), mirrors the cobrança "everything lands for human check" pattern, and keeps Metabase as the station SoT |
| 31 | Unified /pagamentos ledger (2026-07-09) | /pagamentos shows ALL charges (rent + third-party + Enel/EDP energy — dropped the source filter) split into two tabs by billing-account type: "Enel/EDP" vs "Aluguel e outros", each with its own previsto/pago/pendente totals + the spreadsheet header funnels; month picker defaults to "Todos os meses". Energy rows show the provider label (no counterparty); the invoice-number column was renamed "Nota fiscal" (distinct from the "Enviado ao fiscal" flag) | Keep energy on /energia only (rent-only pagamentos) | Gabriel's call ("unify all payments here"): one ledger for every payment. Intentionally overlaps /energia Faturas (the fatura-lifecycle view); totals now include energy |
| 32 | Station declutter — hide list + freshness (2026-07-09) | Two independent /estacoes declutters: (A) a persisted manual hide list (`charging.stations.hidden` + `set_station_hidden` RPC, migration 18; sync never writes `hidden`, so a re-clone preserves human hides; hidden rows dropped by default with a "Mostrar ocultas" reveal chip), (B) a client-side "coleta > 7 dias" freshness filter. Hidden stations are excluded from the KPI strip + quick-filter chip counts so they match the filtered table (decision #16 canonical count) | Manual-only; or freshness-only | Gabriel's call ("both"). Freshness filter is caveated: scraper data is frozen post-clone (#25), so it hides most stations over time |
| 33 | Enel/EDP bill lifecycle "Ciclo" (2026-07-09) | Two statuses side by side on /energia › Instalações: **Status portal** (scraper billStatus, relabeled) and **Ciclo** (nosso, derived — 1 Detectada · 2 Analisada · 3 Enviada ao fiscal · 4 Paga, furthest-point-wins; `lib/energia/ciclo.ts`); row click opens a fatura-history drawer (últimas 12 competências com valor, vencimento, ciclo, comprovante e link do PDF no Drive via o `link_fatura` já clonado). Sem-DA worry (`due_soon_no_auto_debit`) fires from Ciclo 2 (parsed charge, keyed `(account, due_date)`) until the due date — replacing the 7-days-before window; Ciclo 1 (detectada, PDF indisponível) never alarms and reads ≈0 on the frozen clone (live-feed only). AMENDS #29's sem-DA timing; no schema/RPC change | "Situação" DA-aware severity matrix as a third column (dropped by Gabriel); new screen or /mensal upgrade | Spec docs/superpowers/specs/2026-07-09-q11-enel-edp-lifecycle-design.md (approved 2026-07-09); portal status and OUR processing status are distinct facts the team needs side by side; PDFs via plain Drive links (scraper-folder parity, #17) |
| 34 | Scraper → app ingestion feed (2026-07-09) | The Vammo-Enel scrapers dual-write: they KEEP the Google-Sheet append AND POST the same per-installation row-dicts to `POST /api/ingest/scraper` (Bearer `SCRAPER_INGEST_SECRET`, its own secret). Payload `{ provider, installations:[{ installationKey, account, faturas }] }` is assembled into a PARTIAL raw-tabs shape (only the provider's 2 tabs) and run through the existing `normalize.ts` → same rows as a clone. Scoped upsert `runScraperIngest` (`lib/ingest/scraper-feed.ts`): billing_accounts **INSERT-NEW-ONLY** matched by natural key (never updates an existing account → station_id/match_status/counterparty preserved; the POST has no Metabase/Vammo_data); charges dedupe on `enel:{id}:{due}`/`edp:{uc}:{due}` (#20) with the sync-vs-rpc stickiness partition (`toChargeRow includeStatus:false` for human rows); charge station/match enriched from the account's current DB station; utility_account_state/monthly_consumption/charge_energy_details upsert; one `audit_events` row per run. State 1 (Detectada) = account-only, no synthesized charge. Safety guards (post-review): the two preflight reads chunk their keys (500/read) so a large POST can never truncate the result and misclassify an `rpc` charge as `sync` (which would clobber a human status/flags/fiscal); the new-account write is upsert-ignore on the PK (idempotent under a concurrent re-POST, still never updates an existing account); a per-POST cap (1000 installations) rejects oversized batches with a 400; `normalize` issues (dropped/coerced rows) are surfaced in the run stats + audit `detail`. Spec `docs/superpowers/specs/2026-07-09-scraper-ingest-feed-design.md` | Modify the scraper to write Postgres directly; app-side portal polling; sheet-read-only forever | Gabriel: "vincular meu scraper: como eu envio novas informações?" — dual-write keeps the downstream weekly-matching + Slack scripts (sheet readers) working while the app gets fresh data. AMENDS #25 (scraper data no longer frozen once the feed runs — the "future scraper→Supabase feed" #25 anticipated); honors #10 (scraper stays his) + #28 (never creates stations) |
| 35 | /pagamentos "Documento & Associações" (2026-07-09) | Feature D, shipped in two slices. **Part 1**: a read-only "Documento de origem" column on BOTH /pagamentos tabs — energy → `charge_energy_details.faturaDriveUrl` (Drive link), rent/manual → `charges.source_document_id` via the session-checked `/api/files/[id]` proxy — via a pure `resolveDocumentHref` helper; distinct from the payment-proof "Comprovante" column. **Part 2a**: the one-comprovante↔muitas-faturas manager already existed on `/comprovantes/[id]` (ReceiptCard "Conciliar" adds via `record_payment` sharing a receipt_id, BindingsTable removes via `unmatch_payment`) but shipped DISABLED because `getViewerContext()` still read the dormant `user_roles` table → aligned it with `getViewer()` (any @vammo.com, decision #26). **Part 2b**: "Reclassificar tipo…" (existing `reclassify_charge`, kind-only, rent/third-party accounts only, kind targets scoped by account type so a rent charge can't be flipped to pure `energia` — that reattribution needs a counterparty and stays in /revisão) + "Vincular/Desvincular documento" via a NEW `set_charge_document` RPC (migration 19: `is_vammo_user` gate, kind guard rejecting comprovante/foto_medidor/contrato, idempotent, one audit row; does NOT refuse `pago` — the source bill is metadata) + a `DocumentPicker` over source-bill documents, so an incomplete/unlinked webhook document is completed by hand. **Invariant fix (migration 20, AMENDS #29)**: `unmatch_payment` now walks a charge off `pago` when no bound-receipt payment remains (not only on coverage loss) — the app's `pago` stays strictly comprovante-backed, while the scraper/portal "paid" remains a separate displayed status (Gabriel: "são status paralelos"). No "split" (multi-station billers already arrive as per-station charges); no schema/table change (`source_document_id` + the `payments` N:M already existed). Spec `docs/superpowers/specs/2026-07-09-pagamentos-documentos-associacoes-design.md` | A new document-centric bulk-linking screen; a dedicated field-edit RPC; a `split_charge` RPC; keeping the conciliado→confirm gate | Gabriel 2026-07-09 ("ver o documento através de um link" + "editar essa entrada" → for comprovantes add/remove faturas, for boletos edit station/tipo). A research+critique pass found 2a already built (just the gating bug), `set_charge_document` the only new RPC needed, and the #29 hole reachable via the remove flow. Simplicity: reused existing RPCs/components, scoped reclassify to safe kind-only transitions |
| 36 | gerar_mes rent amount = agreed valor_mensal, Metabase only flags (2026-07-09) | Test-env #2. `gerar_mes` now bills the AGREED contract `valor_mensal` for ALL billable types (fixo / por_box / por_box_minimo); Metabase `active_boxes` NEVER changes the billed amount — it only raises INFORMATIONAL flags: `boxes_mismatch` (active_boxes present AND ≠ contract box_count), `no_metabase_data` (active_boxes null), `boxes_stale` (>48h). `valor_por_box`/`min_box` no longer drive the amount (migration 21). The read-side preview `computeGerarMesProjection` mirrors this branch-for-branch — including SQL's NULL semantics (a null contract box_count is NOT a mismatch) — and was fixed to also exclude `rent_manual` contracts (it had diverged from the RPC's filter). Pro-rata unchanged (station.source_created_at, day≥5, /30 base). The "confirmar/editar" model stays: preview (nada é criado até confirmar) → gerar tudo → editar por linha no ledger (`adjustCharge`/`reclassify`/status) — no pre-commit per-row edit. July 2026 was generated live on the test env: 165 rent charges, R$195.629,55, 6 `boxes_mismatch` flagged (each billed valor_mensal), 0 `no_metabase_data`, 0 `pro_rata` (no July-created station on the frozen clone) | Metabase active_boxes drives the amount + flag (prior behavior); inline per-row edit/exclude before generating; add contract `starts_on` as the pro-rata trigger | Gabriel 2026-07-09: on a box mismatch, bill the combined/agreed value and only sinalizar the drift — don't let Metabase silently change the billed rent. Contract stays the source of truth for the amount; drift surfaces for renegotiation. AMENDS the por_box pricing of `gerar_mes`; the projection↔RPC sync + rent_manual fix close a preview-vs-actual divergence |
| 37 | /pagamentos editability + Nova cobrança manual (2026-07-10) | Feedback on the shipped ledger. (a) **Gerar-mês dialog** capped to `max-h-[85vh]` flex column, only the list scrolls (was overflowing). (b) **Editability ("os dois")**: the per-row edit menu was the 20th/last column, off-screen — DataTable gains a `pinnedRightColumnIds` prop (sticky-right column, reusable by any wide table) AND /pagamentos rows open a `PagamentoDrawer` (Sheet) on click with all fields + the same `StatusActions` edit menu (stopPropagation on the menu/links avoids double-firing). (c) **Nova cobrança manual** (`create_manual_charge` RPC, migration 22): create a single aluguel/energia charge by hand — station-only (no contract/counterparty; the schema CHECKs forbid a rent account without a contract and a third_party without a counterparty), so `billing_account_id` stays NULL with `station_id` set + `match_status='manually_matched'` (kept out of the unmatched-charges review queue, which filters on unmatched/needs_review), `source='manual'`, dedupe `manual:{station}:{YYYY-MM}:{kind}` (rejects a duplicate — edit instead), optional source document (kind-guarded). Form on /pagamentos: tipo + station picker + competência + vencimento + valor + método; document bound afterward via the existing row action | Generalize create_manual_bill (energy-only invariant); require a contract for manual rent; inline per-row preview edit; column-hide-only for reachability | Gabriel 2026-07-10: "cannot edit … the window is not well formatted" + "generate a new payment entry (Energia or aluguel) manually … manual station selection and payment method". "Permitir só estação" (station-only). Reuses the existing edit RPCs + pickers; new RPC keeps create_manual_bill intact |
| 38 | Boleto matching by email sender (2026-07-10, test-env #4) | Gabriel: "the most reliable matching method so far is to compare the sender to our list." The n8n cobrança webhook already carries `remetente`; the app now (a) stores the normalized from-address structurally on each charge (`charges.email_sender`; was only free text in notes); (b) keeps a **learned list** `charging.station_senders` (`sender_email` UNIQUE → `station_id`; N senders per station, per "permitir mais de um remetente para cada estação") written only by the `set_station_sender` RPC (migration 23); (c) in `ingestCobrancasPayload`/`resolveAccount`, when the AI gave NO station, falls back to the sender→station mapping (and, for aluguel with no cadastro, attaches to that station's single ACTIVE contract's rent account); (d) **learns on human reclassify** — `reclassifyCharge` best-effort upserts `sender→station` after a charge with a sender resolves to a station. Deterministic, in-app; the n8n AI classification is unchanged (the sender list is the reliable backstop/primary, not a new in-app AI). Everything still lands `needs_review` (decision #27); the association stays editable via the existing reclassify. Follow-ups noted: a senders-list CRUD admin screen; a first-class "Remetente" field in the review UI (today shown via the "Remetente:" note) | counterparties.email; a contract-scoped alias; reusing account_aliases (keys to billing_account only); auto-learning on the AI's own match (risks learning wrong matches) | Gabriel 2026-07-10 "compare the sender to our list … mais de um remetente para cada estação (não existem casos ainda, mas melhor estar preparado)". Sender→station (unique sender) is the point to revisit if a sender ever serves multiple stations |
| 39 | Comprovante per-page isolation (2026-07-10, test-env #3) | Gabriel: "isolar as páginas de PDFs multi-página em páginas individuais armazenadas no supabase" so the EXACT page bound to a charge opens reliably (the browser `#page=N` anchor is fragile). New route `GET /api/files/[documentId]/page/[page]` (session-gated) serves the isolated one-page PDF: **lazy-cached** — served from a private Supabase **Storage** bucket `comprovante_pages` when already isolated, else the whole PDF is fetched from Drive (decision #17 store unchanged), page n is split with **pdf-lib** (`lib/comprovantes/split.ts`, unit-tested), uploaded to Storage + recorded in `charging.document_pages` (PK document_id+page_number), then served. No eager pipeline change / no backfill — pages materialize on first access; re-derivable from Drive if a blob is lost. The comprovante deep-dive ReceiptCard gains a "Ver página" link to it. The whole-PDF proxy + `#page=N` jump stay for context. NB: introduces Supabase Storage for comprovante pages (Drive remains the whole-file store, #17) | Eager split+store in the pipeline (needs a backfill + sync between whole-PDF and page files); on-demand only (no storage — doesn't honor "armazenadas no supabase"); rasterize to PNG (loses selectable text, needs a rasterizer); per-segment cropping (text-split parser can't crop the visual page) | Gabriel 2026-07-10 "página como arquivo próprio … armazenadas no supabase". Lazy-cache honors "stored in Supabase" while keeping Drive as the source of truth (re-derivable) and avoiding an eager backfill. Migration 24 (bucket + document_pages) |
| 40 | Fiscal groundwork: Ciclo on Faturas + fiscal reset + FISCAL-sheet check (2026-07-10) | Three pieces toward the send-to-fiscal flow (Q8). (a) The /energia › **Faturas** tab gains the same "Ciclo" column as Instalações (`FaturaRow.ciclo` via the page's `cicloOf`, decision #33 derivation). (b) **One-time reset** (migration `reset_energy_fiscal_exported`, applied 2026-07-10): all 517 energy faturas flagged `fiscal_exported` (cloned "Financeiro Check") walked back to Ciclo 2 · Analisada — the cloned flags are no longer trusted; "Enviada ao fiscal" must now be EARNED by verification against the real FISCAL sheet. Rent "No Fiscal" flags (342) untouched; Paga rows unaffected (stage 4 ignores the flag). (c) **`lib/fiscal/fiscal-sheet.ts`** — read-only duplicate check `checkFaturaOnFiscalSheet({installationId, dueDate, notaFiscal?})`: reads the FISCAL spreadsheet (`FISCAL_SPREADSHEET_ID`, same read-only SA), tab = due-date month `MM-YYYY`, matches rows by "Consumo de energia - {id}" (+ due date, with nota-fiscal fallback); missing month tab → not registered. Parsing reuses normalize.ts (`parseTimestamp` extended for `DD/MM/YYYY HH:mm:ss`); unit-tested on Gabriel's real Enel/EDP rows. AMENDS #25 as Q12 anticipated: the app reads the FISCAL sheet (never the scraper sheet, which stays severed); still zero sheet WRITES — the send itself is the next step | Trust the cloned fiscal flags as-is; write-time dedupe only (no pre-check); a Supabase mirror of the fiscal sheet | Gabriel 2026-07-10: "To send a fatura to fiscal, first we have to see if it is not already there" + "change all Faturas that are 'Enviada ao fiscal' to Analisada". Sheet id pending (env var placeholder). UPDATE 2026-07-10: the check is runnable from /energia › Faturas ("Verificar no fiscal" button → `verifyFaturasOnFiscal` action → shared `lib/fiscal/check-faturas.ts`, also used by `scripts/check-fiscal.ts`) and now SYNCS the result into the "Enviada ao fiscal" flag — faturas registered on the sheet → `fiscal_exported=true` (→ Ciclo 3), not-registered / "sem aba" (due-month has no tab yet) → false — via the audited energy-scoped `set_fiscal_exported` RPC (migration 30). This is how the flag is EARNED post-reset. FISCAL_SPREADSHEET_ID now set in Vercel |
| 41 | Comprovante flow reformulation — chunked processing + eager pages + hover (2026-07-10) | Gabriel: "remove the n8n processing part; drop a >40-page comprovante → analyze/process → whole PDF to Drive → for each match upload the matched page individually to Supabase; that per-page file shows on hover; process in 10-page steps with a progress bar; cold copy → reset and re-match." Built: (a) **the app owns processing** — the app pipeline already extracted+parsed+matched; removing "the n8n processing part" = Gabriel disabling his `PDF_Comprovante_Processor` n8n workflow (its sheet output was dead post-#25), and the app-side `/api/cron/comprovantes-drive-poll` route + its n8n `*/15` schedule are DELETED (intake = app drag-drop only). (b) **Chunked processing**: upload stores the whole PDF to Drive + a `documents` row and returns `{documentId, pageCount}` (no more 20-page inline cap); the client (`upload-card`) loops 10-page chunks against `POST /api/uploads/comprovante/chunk` → `processComprovanteChunk(from,to)` with a progress bar (migration 27 `pages_processed`; `parseComprovantePages(pages, startPage)` — the mandatory offset fix; `isolatePages` loads the source PDF once). Each chunk re-extracts+slices (reuses the exact tuned `extractPdfText`, zero parse divergence) — bounded work per request, no Vercel upgrade. (c) **Eager per-match page** → the matched physical page is split + uploaded to the `comprovante_pages` bucket at match time (the lazy `/page/[n]` route stays as fallback). (d) **Hover preview** → `ComprovanteChip` shows the isolated page (Base UI `PreviewCard`, `comprovantePageSrc`) on hover; click still deep-links. (e) **Matcher reaches paid charges** → `loadCandidates` = OPEN + `pago`-without-comprovante (so a dropped comprovante binds to the clone's sync/portal `pago` rows, satisfying #29; sync `pago` already carrying a comprovante is excluded). (f) **Reset** → `reset_comprovante_matches` RPC (migration 28) + `resetComprovanteMatches` action + "Resetar comprovantes" inbox button: unbinds every comprovante-backed payment, walks comprovante-driven `pago` back to open (sync/portal `pago` preserved), deletes receipts/pages/comprovante docs, purges the Storage bucket + Drive files — re-drop the same PDFs to re-test. **Vercel: Hobby, no upgrade** (10-page chunks fit the function limit); the only ceiling is the ~4.5 MB request-body cap (accepted — the dropzone rejects larger client-side). AMENDS #17 (whole PDF still to Drive), #22 (drive-poll gone), #39 (per-page isolation now eager at match), relaxes #29's matcher to reach paid rows. **Adversarial-review fixes**: within-batch candidate dedup so a charge is auto-bound by at most one receipt per run (chunk↔whole-doc equivalence, no double-count, boundary-independent); `unmatch_payment` gains a `status_source='rpc'` guard (migration 29) so unbinding a comprovante never reverses a sync/portal `pago` (AMENDS #35); the upload content_hash race returns the winning dedup row (+ deletes the orphan Drive file) instead of a 500; `kind='comprovante'` guard on the chunk/reprocess entry; a "Requer revisão" upload state; proper 404/400/502 codes. Spec `docs/superpowers/specs/2026-07-10-comprovante-chunked-reformulation-design.md` | Direct-to-storage upload for >4.5 MB PDFs (deferred — accepted ceiling); adding a `processing` enum value (kept `pending`, idempotent sweep is the safety net); per-range PDF extraction (chose extract-all-then-slice for zero parse divergence); keep the n8n drive-poll intake | Gabriel 2026-07-10 (full reformulation). Chunking + client-driven loop keeps Hobby viable; matcher-reaches-pago is the least-destructive way to stress-test matching against a clone whose target charges are already sync-`pago`; the reset makes the test repeatable |
| 42 | Send to fiscal — the app WRITES the FISCAL sheet (2026-07-10) | The WRITE half of the fiscal flow, and the ONLY place the app writes a Google Sheet — a SCOPED exception to the #25 sever (the scraper/rent sheets stay severed; only the separate FISCAL sheet, Q12, is written). **Rule** (Gabriel): for each energy fatura NOT already on the sheet — due-year ≤ 2025 → ignore; = 2026 → send IFF `utility_account_state.auto_debit='cadastrado'` (the UI's DA source; `charge_energy_details.auto_debit` reads 'desconhecido' on the clone); ≥ 2027 → BLOCK with the warning "2027 BLOQUEADO - REVER FUNÇÃO" (never written — the function must be re-validated for the year rollover first). **Operation** `sendFaturasToFiscal` (`lib/fiscal/send-fiscal.ts`, server-only): locale-guards the sheet is pt-BR (else throws — the format assumes it), re-runs the fiscal check, then `values.append` (USER_ENTERED + INSERT_ROWS) the eligible faturas to the LAST row of their due-month tab `MM-YYYY`, batched per tab with isolated per-tab error handling; sent faturas → `fiscal_exported=true` (→ Ciclo 3) via `set_fiscal_exported`. **Row** (12 cols, `lib/fiscal/fiscal-row.ts`, pinned to the two real samples): timestamp DD/MM/YYYY HH:MM:SS BRT · `DA` · supplier razão social per provider (Eletropaulo / EDP São Paulo) · valor `,`-decimal no-thousands · nota fiscal · `Consumo de energia - {id}` (Enel) / `... {uc} DA` (EDP) · vencimento DD/MM/YYYY · `401: Charging Infra/Energy: Electricity` · `COGS - 401: …` · (vazia) · unified status **`Upload de Fatura via Eletron - Aguardando Fiscal`** · `=HYPERLINK("{driveUrl}";"Ver Fatura")`. **Safety** (Gabriel: "cannot be mistaken", no preview step): every built row is round-tripped through the READ parser (`selfVerifyRow`) and skipped if it doesn't decode back to the fatura's id/venc/nf/valor (also guarantees the check finds it later → idempotent re-runs never double-send); `valor=charge.amount` (= energy total, verified). **`Enviar ao fiscal em lote`** button (/energia › Faturas) → direct send → toast summary (enviadas / já na planilha / sem DA / ignoradas ≤2025 / 2027 bloqueadas). NEEDS the SA to have **Editor** on the fiscal sheet (`createSheetsWriteClient`, RW scope) + `FISCAL_SPREADSHEET_ID` (Vercel). AMENDS #25 (write half now exists, fiscal sheet only) + completes #40. **Scoping (Gabriel follow-up)**: the send only PROCESSES faturas WITHOUT the check (`fiscal_exported=false`) — already-checked ones are skipped, not re-validated; an unchecked fatura already on the sheet is marked checked (not re-sent). **Manual queue**: a "Conferir manual" button opens the fiscal sheet in a new window + lists the faturas the auto-send skips (2026, sem débito automático, ainda sem check) with PDF links + a per-row "marcar enviada" (`getFiscalManualQueue`/`markFaturasFiscalExported`) — the não-cadastrado are handled by hand. **Refinements (Gabriel follow-up)**: (i) **value-0 faturas** (`amount=0`) are NOT sent — instead auto-checked AND marked `pago` even without a comprovante (a deliberate #29 exception via `settle_zero_value_faturas`, migration 31, guarded to `amount=0`); (ii) faturas whose **due date already passed** (BRT) are NOT sent; (iii) clicking **Enviar ao fiscal em lote runs Verificar first** — one sheet read drives the full `fiscal_exported` sync (on-sheet → true, off-sheet → false) AND the append; the classification is a pure, unit-tested `classifyFaturaForSend`. On the frozen clone (today 2026-07-10) ~380 faturas are past-due so few send — expected (the rule is for live use) | Per-fatura button; a preview/confirm dialog; creating missing month tabs; sending all years; unified vs per-provider status text | Gabriel 2026-07-10 gave the rule + the two exact sample rows ("this cannot be mistaken; review thoroughly"). Chose: unified status ("via Eletron" is traceable), year-scoped guard (2026 only, 2027 hard-blocked), no preview but a round-trip self-verify + locale guard + full pre-deploy review/tests as the safety net |
| 43 | Comprovante matching recovery — parser + matcher + review declutter (2026-07-10, test-env #5) | A 163-receipt rent comprovante matched only 117/148; diagnosis (re-parsed the 46 `needs_review` receipts' stored raw text + re-ran the matcher over the 396-open-rent pool) found the gap was receipt-side parsing + matcher over-competition, NOT missing charge keys. Gabriel approved (AskUserQuestion) 3 fixes + the noise control. **Parser** (`lib/comprovantes/parse.ts`): (a) `extractTedFields` now reads the RECEBEDOR-scoped text (parsePixTedPage passes `text` from "dados do recebedor" onward), parses the Itaú combined `agência/conta: 0444/41193-8`, and returns null for a bank-MASKED CNPJ (`*****435000-**`) / any non-11/14-digit partial — the old code fell back to the PAYER's agência/conta (`074222501`), so 31 receipts matched on a garbage key (removed `extractCnpjFallback`; the Bradesco "Dados da conta a ser creditada" TED layout still parses via the fallback branch, existing PIX/TED tests green); (b) new **branch 5 `parseTitulosPage`** for "Comprovante de Operação - Títulos Outros Bancos" (Valor pago + código de barras + favorecido CNPJ, `boleto_barcode`, utility null) — recovered 8 amounts that read null → "outro" before. **Matcher** (`lib/comprovantes/pipeline.ts` + `match.ts`): `pago`-without-comprovante charges join the candidate pool ONLY for ENERGY (`isEnergyCandidate`); rent/third-party paid prior months no longer compete — AMENDS #41(e)'s "OPEN + pago" pool which, for rent, created open-vs-paid ambiguity. Plus a **prefer-open tiebreaker** — ≥2 survivors with exactly one `isOpen` → pick it. **Review declutter** (`components/comprovantes/review-queue.tsx`): rows split into "precisam de decisão" (candidateIds>0, main table) vs **"Sem correspondência"** (candidateIds empty → collapsed, hidden by default, count + one-click bulk **`reject_receipts`** — migration 32, set-based sibling of #7's `reject_receipt`, same guards + audit) — for Gabriel's "200+ entries, muitas não relacionadas, não posso descartar manual". **Measured end-to-end** (re-parse + re-match on prod data): 117 → **127 auto** (+10, mostly from the open-only rent pool removing paid competition); **16 still ambiguous** (a landlord with N same-value stations → one payment matches N open charges); **20 none** (genuinely unrelated: Bradesco Saúde health plan, suppliers, or no open charge). Ships needing a **reprocess** (reset + re-drop, or Reprocessar) to re-parse the stored receipts. **Follow-up (same day)**: for the 16, Gabriel chose the HUMAN-CONFIRMED variant — a **"Resolver grupo" (N↔N)** control in the review queue (pure `buildResolvableGroups` in `components/comprovantes/resolve-groups.ts`, unit-tested; `resolveReceiptGroup` action loops `record_payment` in one JWT mint). When N ambiguous receipts share the SAME N-candidate set (same key + value) and all have a parsed amount, one confirmed click binds them 1:1 (interchangeable → any bijection settles the same charges); asymmetric buckets (1↔3, 3↔2) stay in the per-row manual flow. The AUTO-pipeline 1:1 variant stays deferred (needs whole-document view under chunked processing). `paymentMethodForReceipt` extracted to a JSX-free `payment-method.ts` so the grouping stays unit-testable; vitest `include` broadened to `{lib,components,app}` | 1:1 AUTO-pairing in the pipeline (deselected as risky — chunked-processing complexity); fuzzy recebedor-NAME matching; a new match_status enum for no-candidate (used candidateIds instead — no migration); client-side loop for bulk reject (chose a set-based RPC) | Gabriel 2026-07-10: "matches 148 … code only matched 117 … review options to match all … brainstorm … look the logs and process data yourself." Diagnosis-first (grounded in the real receipts' raw text), then the parser/matcher fixes + the human-confirmed N↔N resolver he picked; the AUTO variant stays deferred |
| 44 | GT-calibrated matcher: competência por data + descarte por valor + pool revertido (2026-07-10, test-env #6) | The live run after #43 collapsed to **19/148 confirmados** — root cause: #43's rent paid-pool exclusion removed ALL 148 targets (every GT charge was sync-`pago` on the clone; the reviewer had flagged the surface-reduction and it was wrongly accepted). Gabriel supplied a **ground truth** (`context/comprovante-1LBlu3gQ-ground-truth.json`, the 148 2_Pagamentos rows bound to the 05.06 PDF, page→cadastro/competência/valor), two GENERAL rules, and the n8n flow as reference ("regras gerais preferidas, específicas proibidas; teste com o GT antes de deployar"). Built: (a) **pool revert** — candidates = every charge without a bound comprovante (open AND `pago`, all account types; the n8n gate was "Comprovante cell empty"); (b) **rule 2 — data pina a competência**: `pinnedCompetencia` maps [20/MM, 20/MM+1) → MM (day ≥ 20 → same month, < 20 → previous); auto-match ONLY inside the pinned competência, key+value matches in another competência → needs_review ("validação humana"); barcode rank stays exempt; REPLACES the n8n 25/10 window; (c) **rule 1 — descarte por valor**: a receipt whose amount matches NO pool charge (tolerance-aware) → new outcome `discard`, persisted as `rejected` by the pipeline actor (never queued; reprocess skips rejected); null amount never discards (parser failure ≠ alien payment); (d) **rank fall-through** (n8n parity: chave OU cnpj OU ag+conta) — a rank whose hits all fail value/competência yields to the next rank instead of ending the match; out-of-competência key+value kept as the human fallback; (e) **`digitKeysEqual`** — leading-zero-insensitive digit-key equality in pix/cnpj/ag+conta ranks (the clone's numeric cells LOST leading zeros — `1610670000192`; banks zero-pad — `000228202278-56`); never for barcodes; (f) **dot dates** — `extractPixDate` reads "realizada em 05.06.2026" (TED layouts), pinning their competência; (g) upload card shows "descartados". **GT harness** (`lib/comprovantes/gt-harness.test.ts`, runs only with the local gitignored fixtures incl. `gt-pool-snapshot.json` pulled from prod): **146/148 auto-corretos, 0 auto-errados, 0 descartes errados, 0 falsos-auto nas 15 páginas não-GT (12 descartadas)**; the 2 restantes são o par N↔N genuíno (mesma pessoa, 2 estações, mesmo valor) que cai em revisão COM os candidatos certos → 1 clique no "Resolver grupo" (#43). Floors asseridos no teste (≥146 auto, 0 errados). **Review-hardening (mesma data, adversarial multi-lens; GT inalterado 146/0/0)**: (i) o descarte da regra 1 exige o recibo TAMBÉM sem hit de chave — pagamento com juros/multa (chave casa, valor diverge) vai para revisão, nunca é rejeitado invisível; (ii) o check de valor roda contra o pool ÍNTEGRO (não o encolhido pelo batch) — 2º recibo de mesmo valor no lote não é descartado por ordem; (iii) survivor com competência NULA não é eliminado pelo pin (ausência de evidência ≠ divergência) → revisão; (iv) prefer-open só decide dentro de UMA competência — recibo sem data com hits em N meses não é chutado para "o que está aberto"; (v) barcode que casa mas com valor divergente → revisão imediata (nunca deixa um rank mais fraco auto-vincular um irmão); (vi) guards de fronteira nos padrões bare de data + validação mês/dia no pin (data-lixo nunca pina); (vii) risco residual documentado: CPF 11 dígitos ↔ telefone fixo 10 dígitos zero-equivalentes (chaves de telefone reais carregam +55) | Manter exclusão de pagos p/ aluguel (provado errado pelo GT); janela 25/10 do n8n (substituída pela regra 20→20 do Gabriel); pareamento N↔N automático (segue proibido — humano confirma); normalizar zeros no clone via migração de dados (regra geral no matcher é robusta a AMBAS as direções e a futuros clones). AMENDS #43 (pool exclusion reverted; prefer-open kept, agora gated) + #29 (matcher binds paid rent retroactively — the comprovante IS the missing proof) | Manter exclusão de pagos p/ aluguel (provado errado pelo GT); janela 25/10 do n8n (substituída pela regra 20→20 do Gabriel); pareamento N↔N automático (segue proibido — humano confirma); normalizar zeros no clone via migração de dados (regra geral no matcher é robusta a AMBAS as direções e a futuros clones) | Gabriel 2026-07-10: "163 extraidos, 19 confirmados… vou te passar o gabarito… entenda os padrões e teste o código… regras gerais preferidas, específicas proibidas… ache os pagamentos do GT no eletron e teste antes de deployar" + o fluxo n8n de referência. Todas as 6 correções são gerais (formato/dados), nenhuma específica de recibo |

| 45 | Regra única de competência — dia 20 (2026-07-11) | UMA regra canônica para "a que mês pertence esta data", em TODO o sistema: data em [20/MM, 20/MM+1) → competência MM (dia < 20 → mês anterior; dia ≥ 20 → mês da data). Já era a regra do matcher de comprovantes para a DATA DE PAGAMENTO (#44, GT-calibrada); agora também é o fallback de classificação de cobranças a partir do VENCIMENTO — substituindo a regra ≤10/≥11 herdada do n8n (prompt da IA 2 do `boleto_aluguel`, atualizado por Gabriel no n8n). A competência DECLARADA no documento (ref./competência impressa) continua mandando; a regra é só o fallback. Racional: os dois relógios discordavam exatamente em vencimentos dia 11–19 (IA arquivava no mês do vencimento; o pagamento no dia do vencimento pinava o mês anterior) → revisão manual recorrente e permanente para esses locadores; alinhar no 20 faz cobrança e pagamento derivarem o mesmo mês (auto-concilia), converge o dedupe `pag:{cadastro}:{YYYY-MM}:aluguel` com o gerar_mês (#27), e obedece "uma definição por conceito" (#16). Não retroativo (cobranças já classificadas ficam; o fallback humano fora-de-competência cobre). Implementação app-side: `pinnedCompetencia` (match.ts) é A função canônica — as Peças 1–2 do intake de boletos (spec 2026-07-11) devem reusá-la, nunca reimplementar. Risco residual aceito: locador que cobra mês corrente com vencimento 11–19 fica com o rótulo deslocado em 1 mês (conciliação funciona igual; reclassificar corrige o rótulo) | Manter ≤10/≥11 só na IA (mismatch permanente com o pin); alinhar tudo no 10 (contraria a regra GT-calibrada do matcher, que é a testada) | Gabriel 2026-07-11: "Topo" após a explicação dos dois relógios e do vão 11–19 |

| 46 | Intake de boletos por e-mail — Peças 1-2: fila "A pagar" + propostas de unificação (2026-07-12) | Primeiras duas peças do spec `docs/superpowers/specs/2026-07-11-boletos-email-intake-design.md`, sobre o webhook `POST /api/ingest/cobrancas` (#27) já operacional. **Peça 1 — fila "A pagar"** (`components/pagamentos/a-pagar-panel.tsx`, 3ª aba de /pagamentos): fila operacional de contas a pagar — toda cobrança em aberto (status `pendente`/`boleto_recebido`/`atrasado`) ordenada por vencimento, com o código de pagamento (linha digitável ou chave PIX) copiável em 1 clique para execução manual no banco (sem integração bancária, por decisão do Gabriel); cobranças SEM estação (caso DIA) aparecem — são pagáveis antes de identificar, com atalho "Identificar" → /revisão/cobranças. Vencido é DERIVADO da data (`due_date < hoje` em BRT), não do status, então nenhuma cobrança atrasada some da fila mesmo que o status persistido não vire `atrasado`; KPIs A pagar / Vencidas / Vencem em 7 dias. **Peça 2 — "Propostas de unificação"** (/revisão/cobranças): resolve os "bancos" que a Peça 1 acumula — (a) o mesmo boleto ingerido 2× (1ª entrega UNIDENTIFIED, reentrega MATCHED sobre a cobrança-clone `pag:`), (b) a linha de uma ND vs o boleto só-valor do DIA. `merge_charge_into(p_duplicate_id, p_target_id, p_reason)` RPC (SECURITY DEFINER, migration `20260712201445_merge_charge_into` + hardening `merge_charge_into_reject_terminal_target`): a duplicada DOA o instrumento de pagamento/documento/vencimento ao alvo (coalesce, nunca sobrescreve) e vira `cancelada` sticky — nunca deletada (FKs RESTRICT + trilha de auditoria: 2 audit_events). Builder puro `buildUnifyProposals` (`components/revisao/unify-proposals.ts`, unit-testado): tier1 `mesmo_documento` (mesmo source_document_id + valor ±0,01), tier2 `remetente_valor` (CNPJ leading-zero-insensível OU remetente exato + mesma competência + valor + alvo COM estação); exatamente 1 candidato no tier mais forte, senão fica na fila manual; 1 duplicada por alvo. Sempre confirmação HUMANA, 1 clique (espelha o "Resolver grupo" N↔N do #43). **Endurecimento pós-review adversarial (workflow, 5 achados confirmados; 2 refutados)**: (crítico) `sameEntity` — nunca propõe merge entre estações DIFERENTES nem entre kinds diferentes (o caso mesmo-locador/N-estações/mesmo-valor do #4/#44 cancelaria a estação errada); só duplicada sem estação (ainda não identificada) ou na mesma estação/kind; (maior) energia em débito automático (`autoDebit='cadastrado'`) é EXCLUÍDA da fila "A pagar" e dos totais — o banco paga sozinho, não é pagável manual (energia SEM DA continua na fila); (menor×2) RPC e builder rejeitam `nao_aplicavel` como alvo E como duplicada (backstop = UI); (menor) as 3 queries de merge-target paginam via `readAll` (mesmo disciplina do scraper-feed contra o teto de linhas do PostgREST) | Um endpoint de candidatos substituindo leituras de 1_Cadastro (Peça 3, adiada p/ fase 2/opcional); painel document-cêntrico de bulk-linking; deixar o matcher de comprovantes resolver duplicatas (é 1↔1 recibo↔cobrança, não cobrança↔cobrança); janela ≤10/≥11 (já resolvida no #45) | Gabriel: "proposta automática + one-click review, sem integração bancária, com o corpo do e-mail" + "se não conciliar, banco para vínculo manual ou comparação com novos documentos (DIA manda boletos sem info, depois uma ND concilia)". Reusa `pinnedCompetencia` (#45), os RPCs/pickers de reclassify (#35/#38) e o padrão de card N↔N (#43); merge_charge_into é o único RPC novo |

| 47 | Staging de boletos de e-mail — aba "Documentos de e-mail" em /pagamentos (2026-07-13) | O ingest do webhook fica INTOCADO, mas as cobranças que ele CRIA viram STAGING: escondidas das abas do ledger (Enel/EDP, Locação, A pagar) até um humano validar e clicar **"Enviar para Pagamentos"** — ou **"Descartar"**. Nova 4ª aba de /pagamentos agrupada POR DOCUMENTO (1 PDF → N cobranças, caso ND), com PDF/remetente/data, chips de prontidão (sem valor/estação/vencimento/método — consultivos, aprovar nunca é bloqueado: boleto DIA é pagável sem estação), badge "convergida" e badge vermelho de pendentes na aba E no item Pagamentos da sidebar. **Dois predicados canônicos** (`components/pagamentos/email-docs-groups.ts`, puro/unit-testado): `isStagedEmailCharge` = `source='email_ai' AND match_status='needs_review'` (a convergência do ingest nunca toca `source`, então `email_ai` = criada-pelo-webhook; convergida gerar_mes NUNCA é escondida do ledger) e `isEmailDocRow` = needs_review E (email_ai OU documento anexado com `documents.source='email_ai'`) — a aba também mostra as convergidas para o clique de revisão; o badge (`countEmailDocPending`) aplica O MESMO predicado (aba e badge nunca divergem). **Enviar** = `reclassify_charge` (aprovar como está); em lote por documento = `approveCobrancas` (1 mint, loop, continua em falha — padrão #43; ensina remetente→estação por sucesso, #38). **Descartar** = RPC `discard_charges` (migration 35, set-based espelho do `reject_receipts` + retire do `merge_charge_into`): elegível SÓ `source='email_ai'` + em revisão + não-terminal + sem pagamentos → `cancelada` sticky + `manually_matched` + audit por id; skip-not-error; NUNCA deleta — o dedupe_key sobrevive, reentrega do n8n converge na cancelada e o guard terminal (H4) impede reabrir; testado em prod (elegível=1, re-discard=0, gerar_mes=0, com-pagamento=0). Convergidas não são descartáveis — remédio = "Desvincular" (`set_charge_document(null)`). **Editor compartilhado** `ChargeEditorDialog` (extraído do ReclassifyDialog; /revisão/cobranças usa o mesmo) com campos de instrumento CONDICIONAIS ao método (`visiblePaymentFields`: transferência→banco/agência/conta; pix→chave; boleto→linha digitável) e vencimento editável — `reclassify_charge` ganhou `p_due_date` (migration 36, DROP+recreate para não criar overload no PostgREST). Decisões do Gabriel (AskUserQuestion): destino = ledger do app (zero Google Sheet); esconder até aprovar (AMENDS #46: boleto DIA só chega em "A pagar" após 1 clique — ponto cego aceito, mitigado pelos badges + chips de urgência na aba); badge aba+sidebar; /revisão/cobranças mantida completa (e-mails aparecem nas DUAS superfícies). **Endurecimento pós-review adversarial (workflow, 8 achados confirmados; 4 refutados como pré-existentes)**: (maior) status terminal SAI do staging — o matcher de comprovantes flipa uma cobrança em staging para `pago` SEM tocar match_status; sem o guard, dinheiro pago sumia do ledger para sempre e a linha ficava presa na aba com Enviar E Descartar em beco-sem-saída (`UNSTAGED_STATUSES` nos dois predicados + no badge); (maior) cobrança com chave `pag:` NUNCA é descartável — um aluguel email-first MATCHED possui a chave canônica do gerar_mês; cancelá-la faria o ON CONFLICT DO NOTHING do gerar_mês engolir o aluguel do mês silenciosamente (guard `dedupe_key !~ '^pag:'` no RPC — hardening `discard_charges_pag_guard` — + `isDiscardableEmailCharge` na UI, que oferece Desvincular no lugar); (menor×6) `reclassify_charge` recusa cobrança cancelada (aprovar de aba stale falhava mudo — hardening `reclassify_refuse_cancelada`); paginação com `.order(id)` estável no badge e na fila; DiscardDialog só fecha em sucesso; falha parcial do "Enviar todas" carrega a mensagem real do RPC (e falha-total vira erro); dica "campos em branco mantêm o valor"; instrumento extraído pela IA exibido read-only quando o método o esconderia. Riscos aceitos documentados: /mensal, /energia e demais consumidores do snapshot ainda contam valores em staging (fora do escopo; estender é trivial com o predicado exportado); limitação pré-existente do RPC coalesce — trocar pix→boleto não LIMPA a chave antiga (campo some do editor mas persiste na cobrança). **Follow-up rastreabilidade (2026-07-13)**: cada documento recebido pela API é etiquetado com TODOS os e-mails envolvidos (`documents.email_context jsonb` = `{addresses:text[], remetente_raw}`, migration 37) — antes só o 1º externo sobrevivia (`charges.email_sender`, p/ matching) e a lista completa ficava só na auditoria. `parseInvolvedAddresses` (todos os endereços do `remetente`, incl. @vammo.com, deduped) + `buildEmailContext`; capturado no insert e UNIONado na reentrega (`unionEmailContext`/`mergeDocumentEmailContext`, best-effort — nunca falha o ingest) para acumular a proveniência quando o mesmo PDF chega de um novo forward. Exibido no card da aba (ícone Mail), na sub-linha da fila /revisão e no editor (read-only "Recebido via"). Escopo por AskUserQuestion do Gabriel: só endereços (zero mudança no n8n — o `remetente` já chega), assunto/corpo ficam para depois (o jsonb é future-proof). Risco aceito: lost-update sob 2 forwards de endereços DIFERENTES do mesmo PDF interleaved (perde no máx. 1 endereço num campo não-autoritativo; a reentrega do MESMO PDF tem endereços idênticos → sem escrita, sem race) | Tabela de staging separada tipo contract_intake (quebraria a convergência #20/#27 e o dedupe — o charge precisa existir para convergir); esconder também as convergidas (faria linhas do gerar_mês sumirem do ledger); DELETE físico no descarte (reentrega recriaria a cobrança; cancelada + dedupe vivo é reentrega-safe); reusar update_charge_status para descartar (cancelada é admin-gated lá e não vira match_status) | Gabriel 2026-07-13: "tab Email Documents pré-processados… one-click review para a Pagamentos sheet… muito editável… Discard… círculo vermelho com contagem… forma de pagamento editável (transferência precisa de agência/conta, boleto do código)" + 4 respostas de AskUserQuestion |

| 48 | Novo contrato — upload app-owned + fluxo numa página só (2026-07-13) | REFINA o intake de contratos do #30 (Q10: `/api/ingest/contratos` + `contract_intake` + `confirm_contract_intake` já existiam): agora o APP é a porta de entrada. Fluxo numa página só (`/alugueis/novo`, `components/contratos/novo-contrato-flow.tsx`): solta o PDF → o app envia ao Drive (pasta Contratos_Aluguel que o n8n já observa) + cria a linha `documents` + uma `contract_intake` em NOVO estado `awaiting_extraction` (migration 38, DROP+re-add do CHECK) → a página faz POLL (`pollContractIntake`, 4 s) enquanto o gatilho do Drive no n8n extrai e POSTa ao MESMO endpoint `/api/ingest/contratos` (inalterado) → o ingest casa o documento por `content_hash` + o intake por `document_id` e o promove → `pending` com os campos da IA (`lib/ingest/contratos.ts`, agora promove `pending` OU `awaiting_extraction`) → a página preenche o formulário revisável com a 1ª PÁGINA do contrato ao lado (`PdfViewer page=1`) → confirmar cria o contrato (RPC `confirm_contract_intake` inalterado). Upload em `POST /api/uploads/contrato` (espelho do de comprovante: same-origin→@vammo.com→operador, dedupe por content_hash, 23505-race, retorna `{intakeId, documentId, status}`). Nova `contratos` DriveFolderKind → `DRIVE_CONTRATOS_FOLDER_ID` (só lookup de env; `documents.drive_folder_kind` grava `'other'` porque o enum do DB não tem 'contratos' — evita migração de enum). Formulário `ContractIntakeFields` EXTRAÍDO do ConfirmDialog do /revisão e reusado pelos dois lugares (nada reescrito). Os 24 campos da IA que o `Fill_Cadastro_Form` já extrai são consumidos SEM adição — o payload n8n é `{drive_file_id, webViewLink, nome_arquivo, extractedData:<JSON da IA>}` + Bearer `N8N_INGEST_SECRET`. **Endurecimento pós-review adversarial (1 achado real + 2 menores)**: (crítico/sem-recuperação) índice único parcial em `contract_intake(document_id)` (migration 39) — sem ele, 2 uploads concorrentes do MESMO PDF podiam encravar 2 linhas `awaiting_extraction` por documento e o `.maybeSingle()` do ingest passava a dar 500 eterno (invisível na fila/badge); `stageIntake` já reonlia o vencedor no erro de insert; (menor) toast duplicado "Contrato criado" removido; (menor) dependência documentada: o promote só funciona porque o n8n re-POSTa o `drive_file_id` original com os mesmos bytes. Decisões do Gabriel (AskUserQuestion): tudo numa página (não na fila /revisão); estado de "loading/processando" com poll enquanto a IA roda; upload na pasta Contratos_Aluguel existente. /revisão/contratos e o webhook n8n-first seguem funcionando (intakes de lá continuam caindo `pending`). **Follow-up — vínculo manual (2026-07-13)**: um toggle "Extrair com IA | Preencher manualmente" no /alugueis/novo para vincular rápido um PDF a uma estação SEM esperar a extração — `POST /api/uploads/contrato` aceita `mode=manual`, que estagia o intake direto como `pending` (extração vazia) em vez de `awaiting_extraction`; o `stageIntake` (agora devolve `{id,status}`) promove um awaiting→pending se um doc já-enviado for re-submetido em modo manual. O formulário compartilhado abre em branco (defaults) e o confirm cria o contrato na hora. `/alugueis/novo` lê `?mode=manual` e `?station=` (pré-seleciona a estação — serve o "vincular a estações existentes"; o rent-tab de uma estação sem contrato pode deep-linkar). O PDF ainda vai para a mesma pasta, então o n8n pode extrair também — inócuo (o confirm manual manda os valores do humano; se ele confirmar antes, o intake vira `confirmed` e o ingest não mexe) | n8n dono do upload+Drive (o app só recebia — invertido para o app-drop); tabela/estado de rascunho em `contracts` (o enum não tem draft — por isso o staging do #30 existe); criar o contrato direto sem revisão humana (viola #8); página de upload + confirmação na fila /revisão (o Gabriel quis tudo numa tela) | Gabriel 2026-07-13: "Novo contrato button… dropa o PDF, o app envia ao GDrive e Supabase, o n8n dispara do upload no Drive e manda a API com a info pré-processada… 1ª página do contrato legível na janela para editar e confirmar" + 3 respostas de AskUserQuestion. **Pré-requisito do Gabriel**: dar acesso de ESCRITA (Content Manager) à service account na pasta Contratos_Aluguel (o #30 só deu leitura) + setar `DRIVE_CONTRATOS_FOLDER_ID` no Vercel + trocar a cauda do n8n (Build Pre-filled URL + Slack → um HTTP Request POST ao endpoint) |

| 49 | Feed do scraper + matching por endereço (2026-07-13) | Duas peças ligando o scraper Vammo-Enel ao app. **(a) Dual-write do scraper** (decisão #34, agora implementado no repo Vammo-Enel): novo `app_feed.py` (`post_installation(provider, account, faturas)`, best-effort, NUNCA levanta) + hooks aditivos e protegidos (import com fallback no-op) em `scraperEnel.py` (POST no fim de `persist()`, conta montada de `self.res`; faturas acumuladas em `process_bill`) e `scraperEDP.py` (POST após `scrape_station()`, `self.info`; faturas em `download_bill`). **EXATAMENTE 1 POST por estação raspada**, que agrupa o estado da conta (eventos: nova/atualização de estação Enel/EDP) + as faturas novas daquela estação no array `faturas` (eventos: fatura nova Enel/EDP NÃO são chamadas separadas — vão juntas no POST da estação); estações puladas (recently-scraped/bloqueadas/erro) não POSTam. Envia strings CRUAS (o `normalize.ts` do app parseia). Env: `SCRAPER_INGEST_SECRET` no Vercel + `APP_INGEST_URL`/`APP_INGEST_TOKEN` em `credentials.json` (needs REDEPLOY p/ a var valer — 401 até lá). **(b) Sugestão de match por endereço** (`lib/matching/suggest.ts`): o matching in-app (`/revisão › Instalações`, RPC `assign_station_to_account`, decisão #34) já substituía o Slack, MAS as sugestões eram só geodésicas e o feed manda `address` sem lat/lon → instalação nova do feed ficava sem sugestão. Novo `suggestByAddress` (Jaccard sobre tokens de endereço normalizados — deaccent + stoplist de tipo-de-rua/cidade/conectores, + boost por número de porta compartilhado; `MIN_ADDRESS_SCORE=0.2`, `HIGH_ADDRESS_SCORE=0.5`) + `suggestMatches` (geodésico se há coordenadas, senão fallback por endereço). `MatchCandidate` ganhou `method: 'geo'\|'address'`, `distanceM`/`addressScore` nuláveis; a UI (`match-actions`) mostra `Nm` (geo) ou `%` (endereço) e o endereço da estação no title. Estações `hidden` saem do pool de sugestão (mas seguem escolhíveis na busca manual "Outra"). NUNCA auto-vincula por endereço (humano confirma sempre, #8). Com isso o Slack matcher (`writeMergedDTBSlack.py`) pode ser desligado | Mandar lat/lon no feed (acopla ao geocode semanal; coords só existem depois dele); só busca manual (sem sugestão p/ o feed novo — pior UX); auto-link por similaridade de endereço (impreciso demais, viola #8); 1 POST por fatura separado do da estação (bundling é atômico + idempotente por `dedupe_key`) | Gabriel 2026-07-13: "smoke test… confirme 1 api call por evento… implemente o matching no app, me faça largar o Slack trigger, como ponto de revisão no app" + AskUserQuestion (sugestão por endereço). Pré-requisito: REDEPLOY do app p/ `SCRAPER_INGEST_SECRET` valer; conferir que o token em credentials.json == Vercel |

| 50 | Pro-rata por box-dia no gerar_mes (2026-07-13) | Quando os boxes de uma estação sobem no meio do mês (ex.: 3 boxes até o dia 14, +3 no dia 14), o aluguel é cobrado proporcional aos box-dias reais em vez do preço cheio. **Base = valor do contrato** (valor_mensal), **escalado por `min(1, Σ box-dias / (30 × N))`** onde N = boxes PRESENTES no mês; cada box presente contribui seus dias ativos no mês (box ativo antes/durante o mês todo = 30; ativado no dia D = 30−D+1, cap 30). Como N conta só os boxes presentes, uma falta permanente (5 de 6 contratados, todos o mês todo) dá fração 1 → **valor_mensal cheio + flag boxes_mismatch** — "sempre use o valor do contrato", só a rampa temporal reduz (Gabriel). Só para `por_box`/`por_box_minimo`; **SUPERSEDE** a pro-rata de estação-nova (`source_created_at`) para esses quando há dados de box; `fixo` mantém a source_created_at. Dados: `stations.box_activations jsonb` (migration 40) = array das datas BRT de primeira ativação dos boxes ativos (uma por box), populado pelo `runMetabaseSync` do card 28556 (`fa.first_active_ts` → data BRT via `toBrtDate`, full-replace diário, sem cabinet_id — a matemática só precisa das datas). Armazenar a DATA BRT (não o timestamp) deixa a conta timezone-free e **idêntica entre o RPC SQL e o preview TS** — cross-check confirmou SQL=TS (exemplo do Gabriel: 141 box-dias, fração 0,7833). Função pura `computeBoxDaysProrata` (`components/pagamentos/box-prorata.ts`, unit-testada, 8 casos) espelhada branch-for-branch no `gerar_mes` (migration 41) e no `computeGerarMesProjection`. Flags append-only (nunca perdidas; on-conflict-do-nothing preserva cobrança existente); base do box-dia gravada em `charges.raw` p/ rastreabilidade; auditoria inalterada. Rollout seguro: até o sync popular box_activations (null), o comportamento é idêntico ao #36 (cai na source_created_at); e o gerar_mês tem PREVIEW antes de criar qualquer cobrança | cabinet_id como chave de tabela por-box (desnecessário — a pro-rata só precisa das datas; Gabriel questionou); denominador = box_count do contrato (daria desconto em falta permanente — rejeitado, N=presentes mantém #36); guardar timestamp cru (risco de divergência TZ entre SQL e TS); log de mudança de contagem forward-only (o first_active_ts é fato histórico, dá box-dias exatos direto) | Gabriel 2026-07-13: "saber toda vez que um box é ativado numa estação, pra pro-ratear (3×30 + 3×16)/(30×6)" + AskUserQuestion (base=contrato, dia 30−D+1) + "why cabinet id?" (simplificado p/ só datas) |

---

## Product Decisions

| # | Decision | Chosen | Rationale |
|---|---|---|---|
| P1 | Users | Finance/charging team | Confirmed by Gabriel; reads open to any @vammo.com account, writes role-gated from Phase 2 |
| P2 | Information density | Excess over lack: dense tables with column-visibility menus, raw source payloads retained (`raw` jsonb), per-row freshness timestamps as first-class UI | Gabriel: "I rather have excess of information than lack of it in the first versions" |
| P3 | Meter readings | New module; photo is structurally mandatory (submit disabled without photo; NOT NULL FK in Phase 2 schema); mobile-first capture flow; EXIF (time/GPS) extracted for verification then stripped | Some stations require monthly physical meter reads; photo requirement came as a hard requirement |
| P4 | Alerts | The app's alerts panel is the record (7 rule categories, acknowledge/resolve lifecycle); Slack becomes a thin push only (vencidas digest + shutdowns → ChargingOps) | Slack messages scroll away; permanent queryable panel replaces re-alerting every 3 days |
| P5 | Language | UI copy in pt-BR, informal "você", sentence case, no emoji; code/identifiers in English | Vammo DS voice/tone; team is Brazilian |

---

## Open Questions / Future Decisions

| # | Question | Status |
|---|---|---|
| Q1 | Phase 2: write `Comprovante`/`Financeiro Check` back to the sheets during transition, or cut the n8n comprovante flow over entirely? | RESOLVED by decision #25 (2026-07-08): full cutover — the app never writes any sheet again |
| Q2 | Production domain (eletron.vammo.com?) | Open — Vercel project itself is created and named `eletron`, connected to GitHub + linked locally (decision #14); custom domain still unset |
| Q3 | Vercel Pro `maxDuration=300` for cron routes (Phase 2 sheet-sync) | Confirmed **Hobby** (2026-07-10, `vercel api /v2/user` → plan=hobby). No upgrade needed: the daily cron is the only Vercel cron, and comprovante processing moved to client-driven 10-page chunks (each request seconds-long, under the function limit — decision #41). The only platform ceiling is the ~4.5 MB request-body cap on uploads (identical on Pro; accepted). Fluid Compute assumed on for the 300 s daily cron |
| Q4 | `@leopardaelectric/vammo-ui` install access (private GitHub package) and current version | Open — resolve during scaffold; fall back to shadcn/ui equivalents if blocked, keeping the same visual tokens |
| Q5 | DIA store↔station and Hubees address↔station mappings: who owns/maintains them (admin screen exists in design) | Open — Phase 3 concern |
| Q6 | Quick n8n hygiene: move hardcoded LlamaParse key (boleto_aluguel) + Arqia portal password (SIM_Data_Arqia) into n8n credentials/variables | Pending — 10-min manual fix in the n8n UI, independent of this repo |
| Q7 | `context/` folder (real bills with CNPJs/bank data) is gitignored, kept local as design fixtures | Decided for now — revisit if the repo needs shared fixtures (then use sanitized copies) |
| Q8 | Finance "sent to finance sheet" flag (Gabriel 2026-07-09): model + source | RESOLVED (2026-07-09): col R header = "No Fiscal" (a TRUE/FALSE/blank boolean). Modeled as charge-level `charges.fiscal_exported` (migration 17, backfilled from `charge_energy_details`), captured in `normalize.ts` from `2_Pagamentos` "No Fiscal" (rent, `parseBoolean`) + Faturas "Financeiro Check" (energy); surfaced as the "Enviado ao fiscal" column (NOT paid — #21). Send-to-fiscal mechanism still deferred; rent flags populate on the next clone |
| Q9 | "Paid ⇔ comprovante" (Gabriel 2026-07-09): a charge is `pago` only when a comprovante/receipt is bound | RESOLVED by decision #29 (2026-07-09): matcher → `pago`; `record_payment` gives baixa only with a bound receipt (migration 14); worry rules suppressed when paid |
| Q13 | Unsupported comprovante format "Comprovante de pagamento de boleto" (bank boleto-payment receipt, seen in the 07.07 fixtures — ~118/522 receipts) | OPEN — neither the app nor the current n8n flow parses it (different `Valor` layout); those pages yield amount=null → needs_review. Port a parser branch for it if it's a recurring format Gabriel needs, or leave to manual review |
| Q10 | Contract onboarding (Gabriel 2026-07-09): manual "new location/contract" form + drop-a-PDF → n8n AI pre-fill → human confirm | RESOLVED by decision #30 (2026-07-09): `POST /api/ingest/contratos` webhook + `contract_intake` staging + `/revisao/contratos` confirm flow (migration 16). App attaches to existing stations only (no station creation, #28). Setup left for Gabriel: grant the SA read on the Contratos_Aluguel Drive folder + swap the n8n final node to the webhook |
| Q11 | Enel/EDP monthly bill lifecycle (Gabriel 2026-07-09): detected → downloaded → registered → paid, per station per month, with DA-aware alerting | RESOLVED by decision #33 (2026-07-09): "Ciclo" (Detectada · Analisada · Enviada ao fiscal · Paga) side by side with the portal status on /energia › Instalações + history drawer with Drive PDF links; sem-DA alert fires from Ciclo 2 |
| Q12 | Finance-sheet export partially reverses the sever (#25) | NOTE — the export writes the FISCAL sheet (not the scraper sheet), so #10/#25 "scraper sheet severed" still holds; the READ half (duplicate check) landed as decision #40 (2026-07-10); record the WRITE amendment when the send is built (Q8) |
